import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `"spoof trial", sans-serif`,
    body: `"Metro Sans", sans-serif`,
  },
});

export default theme;
